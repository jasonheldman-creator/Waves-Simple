import streamlit as st
import pandas as pd

st.set_page_config(page_title="Waves Simple Console", layout="wide")

st.title("🌊 WAVES INTELLIGENCE — Simple Console")
st.write("Upload a CSV and view the data instantly. This is the clean base version.")

uploaded_file = st.file_uploader("Upload Wave Snapshot (.csv)", type=["csv"])

if uploaded_file:
    df = pd.read_csv(uploaded_file)
    st.success("File loaded successfully!")
    st.dataframe(df)
else:
    st.info("Please upload a CSV file.")