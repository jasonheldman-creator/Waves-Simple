# Public site URL - used for canonical URLs, Open Graph tags, and sitemap
NEXT_PUBLIC_SITE_URL=https://www.wavesintelligence.app

# Console redirect URL (optional) - if set, /console page will redirect to this URL
# NEXT_PUBLIC_CONSOLE_URL=

# Live snapshot CSV URL (optional) - external URL for wave performance data
# If set, WaveCards will fetch from this URL first, then fall back to /api/live_snapshot.csv
# Example: https://example.com/data/live_snapshot.csv
# NEXT_PUBLIC_LIVE_SNAPSHOT_CSV_URL=
