# Public site URL - used for canonical URLs, Open Graph tags, and sitemap
NEXT_PUBLIC_SITE_URL=https://www.wavesintelligence.app

# Console redirect URL (optional) - if set, /console page will redirect to this URL
# NEXT_PUBLIC_CONSOLE_URL=

# Live Snapshot CSV URL (optional) - external CSV endpoint for wave data
# When set, this is the PRIMARY data source for wave snapshots
# Falls back to /api/live_snapshot.csv if unavailable, then to deterministic demo data
# NEXT_PUBLIC_LIVE_SNAPSHOT_CSV_URL=https://example.com/path/to/live_snapshot.csv
