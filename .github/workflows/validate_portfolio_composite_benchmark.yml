# Validate Portfolio Composite Benchmark

# Apply cache-busting steps
- name: Cache Busting
  run: |
    echo "Cache Busting..."
    # Add your cache-busting commands here

# Apply Python cache-clearing steps
- name: Clear Python Cache
  run: |
    echo "Clearing Python cache..."
    find . -type d -name '__pycache__' -exec rm -r {} +

