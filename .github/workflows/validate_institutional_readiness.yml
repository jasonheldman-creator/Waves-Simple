# Updated GitHub workflow to add permissions and enhance error handling

# Permissions
permissions:
  contents: read
  pull-requests: write
  issues: write

# Rest of the content
...

      - name: Comment validation status on PR
        id: comment-validation
        continue-on-error: true
        run: |
          try {
            // Existing github.issues.<create_comment_logic>
          } catch(error) {
            core.setFailed(error.message);
            console.error("Failed to create comment", error);
          }