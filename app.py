# Example content before the narrative update
# The content below is updated as per your directives.

def alpha_narrative():
    # Replace the existing narrative
    narrative = (
        "Reported alpha reflects security selection efficiency at comparable exposure.\n"
        "Total excess return is primarily driven by regime-aware exposure management and capital preservation, which are intentionally classified outside traditional alpha."
    )

    label = "Comparable-Exposure Selection Alpha"  # Updated label

    overlays = (
        "VIX-based overlays, Regime gating, and Exposure reduction / cash sweeps are explicitly referenced. "
        "Overlay-driven preservation effects are reported separately to avoid double-counting as alpha when capital at risk differs from the benchmark."
    )

    return narrative, label, overlays